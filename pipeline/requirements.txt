# Python 3.13. Install with:  python -m pip install -r pipeline/requirements.txt
pypdf>=4.0      # text-layer extraction for recover_lines.py
pytest>=8.0     # python -m pytest pipeline -q
python-docx>=1.1  # docx_to_md.py
pdfplumber>=0.11  # word coordinates for extract_margins.py (margin glosses + line numbers)
